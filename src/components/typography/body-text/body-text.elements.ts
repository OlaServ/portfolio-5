import { styled } from "@chakra-ui/react";

const Paragraph = styled("p", {
  baseStyle: {
    fontSize: "16px",
    fontWeight: "200",
  },
});

const Span = styled("span", {
    baseStyle: {
      fontSize: "16px",
      fontWeight: "200",
    },
  });

export const BodyTextElements = {
  Paragraph,
  Span,
};
